# calc

A tiny pure-Python arithmetic CLI. **Not an AI system** — no models, no machine
learning, no inference, no agents. This fixture verifies that the scanner marks
non-AI projects as out of EU AI Act scope instead of reporting a (misleading)
compliance percentage for them.
